{
  "version": 2,
  "framework": "vite",
  "rootDirectory": "booking-app",
  "buildCommand": "npm run build",
  "installCommand": "cd booking-app && npm install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
