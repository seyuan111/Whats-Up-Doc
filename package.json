{
  "name": "booking-appointment-app",
  "private": true,
  "scripts": {
    "dev": "cd booking-app && npm run dev -- --host 0.0.0.0",
    "build": "cd booking-app && npm run build",
    "preview": "cd booking-app && npm run preview"
  }
}